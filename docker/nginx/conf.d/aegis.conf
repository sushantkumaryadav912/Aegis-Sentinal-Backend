upstream aegis_backend {
    server aegis-backend:8080;
    keepalive 32;
}

server {
    listen 80;
    listen [::]:80;
    server_name _;

    # Certbot webroot ACME challenge route
    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    # Redirect HTTP to HTTPS in production once SSL is issued
    location / {
        proxy_pass http://aegis_backend;
        proxy_http_version 1.1;

        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_set_header Connection "";

        # Rate limiting
        limit_req zone=api_limit burst=20 nodelay;
    }
}

# HTTPS Server block (Uncomment once Let's Encrypt certificates are acquired)
# server {
#     listen 443 ssl http2;
#     listen [::]:443 ssl http2;
#     server_name api.aegissentinel.io;
#
#     ssl_certificate /etc/letsencrypt/live/api.aegissentinel.io/fullchain.pem;
#     ssl_certificate_key /etc/letsencrypt/live/api.aegissentinel.io/privkey.pem;
#     ssl_protocols TLSv1.2 TLSv1.3;
#     ssl_ciphers HIGH:!aNULL:!MD5;
#     ssl_prefer_server_ciphers on;
#
#     location / {
#         proxy_pass http://aegis_backend;
#         proxy_http_version 1.1;
#
#         proxy_set_header Host $host;
#         proxy_set_header X-Real-IP $remote_addr;
#         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
#         proxy_set_header X-Forwarded-Proto $scheme;
#         proxy_set_header Connection "";
#
#         limit_req zone=api_limit burst=20 nodelay;
#     }
# }
