spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/aegis
    username: aegis
    password: aegis_dev

  jpa:
    hibernate:
      ddl-auto: validate

    open-in-view: false

    properties:
      hibernate:
        format_sql: true

  flyway:
    enabled: true

  data:
    redis:
      repositories:
        enabled: false

logging:
  level:
    root: INFO
    com.aegis: DEBUG
    org.springframework.security: INFO
